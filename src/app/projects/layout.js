export const metadata = {
  title: "Projects",
  description:
    "Selected work by Nick van den Dungen — portfolio site, Pong (Three.js + Django), Kotlin REST API, MiniShell in C, and freelance web builds.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
