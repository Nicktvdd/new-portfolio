export const metadata = {
  title: "About",
  description:
    "About Nick van den Dungen — Full-Stack Software Engineer leading engineering at Soil Scout in Helsinki.",
};

export default function AboutLayout({ children }) {
  return children;
}
