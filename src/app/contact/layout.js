export const metadata = {
  title: "Contact",
  description: "Get in touch with Nick van den Dungen — Full-Stack Software Engineer based in Helsinki.",
};

export default function ContactLayout({ children }) {
  return children;
}
